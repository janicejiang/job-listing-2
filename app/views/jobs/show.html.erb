<section id="show_job">
  <div class="container">
    <div class="row">
      <div class="col-sm-9 col-sm-offset-1">
        <h1> <%= @job.title %> </h1>
        <p><%= simple_format(@job.description) %></p>
      </div>
      <div class="col-sm-2">
        <%= link_to("投个简历", new_job_resume_path(@job), class: "btn btn-primary btn-lg") %>
      </div>
    </div>
  </div>
</section>
